import React, { PureComponent, PropTypes } from 'react';

export default class AddDialog extends PureComponent {
  static propTypes = {

  };

  render() {
    return (
      <div className="home-add-dialog">
        Component content: home/AddDialog
      </div>
    );
  }
}
